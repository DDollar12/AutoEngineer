services:
  - type: web
    name: autoengineer
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: autoengineer-db
          property: connectionString
      - key: FLASK_ENV
        value: production
      - key: FLASK_SECRET_KEY
        generateValue: true
      - key: ADMIN_EMAIL
        sync: false
      - key: PAYSTACK_SECRET_KEY
        sync: false
      - key: PAYSTACK_PREMIUM_WEEKLY_PLAN
        sync: false
      - key: PAYSTACK_PREMIUM_MONTHLY_PLAN
        sync: false
      - key: PAYSTACK_PREMIUM_ANNUAL_PLAN
        sync: false

databases:
  - name: autoengineer-db
    databaseName: autoengineer
    user: autoengineer
